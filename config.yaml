llm:
  model: "gpt-4-turbo-preview"
  temperature: 0
  max_tokens: 1000

embeddings:
  model: "text-embedding-3-small"

ingestion:
  chunk_size: 1000
  chunk_overlap: 100

vector_store:
  type: "faiss"
  path: "./vectorstore"

agent:
  type: "react"
  verbose: true
